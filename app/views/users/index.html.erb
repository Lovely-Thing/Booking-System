<% provide(:title, 'All users') %>
<h1>All users</h1>


<%= will_paginate %>
<table class="users">
	<%= render @users %>
</table>

<%= will_paginate %>

<p>
<%= link_to 'New User', new_user_path if current_user.admin? %>
</p>
