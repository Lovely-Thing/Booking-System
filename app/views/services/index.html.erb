<h1>Listing services</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Price</th>
    <th>Duration</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @services.each do |service| %>
  <tr>
    <td><%= service.name %></td>
    <td><%= service.description %></td>
    <td><%= service.price %></td>
    <td><%= service.duration %></td>
    <td><%= link_to 'Show', salon_service_path(@salon, service) %></td>
    <td><%= link_to 'Edit', edit_salon_service_path(@salon, service) %></td>
    <td><%= link_to 'Destroy', [@salon, service], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Service', new_salon_service_path(@salon) %>
