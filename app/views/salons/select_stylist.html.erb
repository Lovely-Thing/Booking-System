
<h1>Book Appointment at <%= @salon.name %></h1>


<h3>Select a Stylist</h3>
<% @salon.employees.each do |employee| %>
	<div>
		<%= link_to image_tag(employee.stylist.image_url(:thumb).to_s) +
			employee.stylist.name, new_appointment_path(employee_id: employee.id) %>
	</div>

<% end %>