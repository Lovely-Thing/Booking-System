<div class="row">
<h1>Book an Appointment</h1>


<aside class="span3">
	<section>
		<h1><%= @salon.name %></h1>
		<h2>Select a Stylist</h2>
		Click on a stylist name or picture to the right. Next you 
		will select the date and time for your appoointment.
	</section>
</aside>

<section class="span4">
<% @salon.employees.each do |employee| %>
	<div class="stylist">
		<div class="stylist_img">
		<% if employee.stylist.image.to_s == "" %>
			<%= link_to image_tag("no_picture.png"), new_appointment_path(employee_id: employee.id) %>

		<% else %>
			<%= link_to image_tag(employee.stylist.image_url(:thumb).to_s), get_services_path(employee_id: employee.id) %>
		<% end %>
		</div>

		
		<div class="stylist_name">
			<%= button_to employee.stylist.name, get_services_path(employee_id: employee.id), 
				{ class: "btn btn-medium", remote: true } %>
		</div>
	</div>
<% end %>
</section>

</div>
